apis:
  main:
    root: openapi.yaml

extends:
  - recommended

rules:
  operation-operationId: error
  operation-operationId-unique: error
  operation-summary: error
  # Локальный сервер указан намеренно; прод-URL появится вместе со стендом
  no-server-example.com: off
